// const { dataBaseConfig } = require("../server");

// var URI = process.env.URI;

// dataBaseConfig.connect(URI, (err, res) => {
//   if (err) {
//     console.log("cant connect to mongoose///");
//   }
// });
